## 1. API: migrating on startup is a setting

- [x] 1.0 Write failing integration tests in `BE/tests/Expenses.Integration.Tests/Persistence/` (or beside `CorsTests.cs` under `Api/`), each against a fresh provisioned but unmigrated database from the fixture, started through `ExpensesApi` with `Environment` and configuration pairs. Tests: *Other environments do not migrate by default* (`Testing`, no setting: no rows in `__EFMigrationsHistory` after the first request), *The setting turns migrating on* (`Testing` with `Database:ApplyMigrationsOnStartup=true`: migrations applied), *Development migrates by default*, *Provisioning is verified either way* (a database created without ICU `und`, with the setting on: the host does not start). Run them and confirm "The setting turns migrating on" fails for the expected reason, i.e. no migrations applied.
- [x] 1.1 In `BE/Expenses.Api/Program.cs`, compute `applyMigrations` from `IsDevelopment()` or the `bool` `Database:ApplyMigrationsOnStartup` (design D2), call `PrepareExpensesDatabase` once with it, and keep the OpenAPI/Swagger mapping Development-only. Update the D15 comment to match. Run the 1.0 tests green.

## 2. API: the Railway environment

- [x] 2.0 Write failing integration tests (in `CorsTests.cs` or a new `RailwayEnvironmentTests.cs`) that start the host with `Environment = "Railway"` and **no** CORS configuration pairs: *The Railway client's origin reads the ledger* (`Origin: https://expenses-frontend-production-2e52.up.railway.app` on `GET /categories` is allowed), *Local development origins are not allowed on Railway* (`http://localhost:5173` not allowed), *Railway migrates on startup* (unmigrated database: migrations applied). Run them and see them fail because `appsettings.Railway.json` does not exist.
- [x] 2.1 Add `BE/Expenses.Api/appsettings.Railway.json` with the single allowed origin and `Database:ApplyMigrationsOnStartup: true` (D1). Add a comment that index 0 can be overridden with `Cors__AllowedOrigins__0`. Confirm the publish output (and so the image) includes it, the way the Development file is included. Run the 2.0 tests green.
- [x] 2.2 Run the whole BE suite (`./test-be.sh`) and confirm the existing CORS, HTTPS and provisioning tests still pass. Report the result counts.

## 3. Client: the preview server answers configured host names

- [x] 3.0 Write failing vitest tests in `FE/src/api/previewHosts.test.ts` for `parsePreviewHosts`: undefined or empty gives `[]` (*Nothing listed still serves localhost*), `" a.example , b.example,"` gives both names (*Several hosts are listed*), and `https://expenses-frontend-production-2e52.up.railway.app/` throws with a message naming `PREVIEW_ALLOWED_HOSTS` and the value (*A URL instead of a host name stops the server*), as do a `host:port` value and a value containing whitespace. Run and see them fail because the module does not exist.
- [x] 3.1 Add `FE/src/api/previewHosts.ts` (browser-free, beside `ledgerAddress.ts`) and make the 3.0 tests pass. If `structure.config.json` rejects the new file, fix that per the `fe-style-fix` skill instead of moving the logic into `vite.config.ts`.
- [x] 3.2 Write a failing wiring check for *A listed public host is served* and *An unlisted host is refused*: a Playwright test or node script (make it runnable from `FE/`) that starts `vite preview` over a build with `API_URL=http://ledger.test:9000` and `PREVIEW_ALLOWED_HOSTS=expenses-frontend-production-2e52.up.railway.app`, requests `/` and `/config.js` with that `Host` header (expect 200, and `config.js` containing the address) and `/` with `Host: elsewhere.test` (expect 403). Run it and see the listed host get 403.
- [ ] 3.3 Set `preview.allowedHosts` in `FE/vite.config.ts` from `PREVIEW_ALLOWED_HOSTS` (shell first, then `loadEnv`) through `parsePreviewHosts` (D4), with a comment pointing at Railway's `${{RAILWAY_PUBLIC_DOMAIN}}`. Run 3.2 green, then `npm run style`, `npm test` and the e2e suite (`./test-e2e.sh`) to confirm localhost preview is unaffected.

## 4. Deployment files (no behaviour: exempt from test-first)

- [x] 4.1 Add `FE/Dockerfile` (single stage, `npm ci`, `npm run build`, `vite preview --host 0.0.0.0 --port ${PORT:-4173} --strictPort`) and `FE/.dockerignore` (D3). Verify locally: `docker build -t expenses-fe FE && docker run -e PORT=8080 -e API_URL=http://localhost:5082 -e PREVIEW_ALLOWED_HOSTS=localhost -p 8080:8080 expenses-fe`, then open `http://localhost:8080` and `/config.js`. Also confirm that running without `API_URL` exits with the `API_URL` message.
- [x] 4.2 Add `BE/railway.json` (Dockerfile `Expenses.Api/Dockerfile`, healthcheck `/units`) and `FE/railway.json` (Dockerfile `Dockerfile`, healthcheck `/config.js`) per D5.
- [x] 4.3 Rewrite `BE/README.md`'s "Migrations" paragraph (Railway opts in with `Database:ApplyMigrationsOnStartup`) and add a "Railway" section: service root and config paths, every variable in the D5 table, the receipts volume and `RAILWAY_RUN_UID=0`, the Npgsql connection-string template (not `DATABASE_URL`), the one-time `01-create-database.sql` step (D6), and why the client uses the public domain and not `expenses-api.railway.internal`.
- [x] 4.4 Update `FE/README.md` "Serving the built client": `vite preview` via `FE/Dockerfile` is the host, plus `PREVIEW_ALLOWED_HOSTS`. Reword the "private network" sentence to say the API is public and unauthenticated. Update the root `README.md` sentence that says production serving is undecided.

## 5. Railway rollout (manual, outside the repo)

- [ ] 5.1 Create the `expenses` database on the Railway Postgres service with `BE/db/init/01-create-database.sql`.
- [ ] 5.2 Configure `expenses-api`: root `/BE`, config `/BE/railway.json`, variables per D5, volume at `/var/lib/expenses/receipts`, public domain `expenses-api-production-4d79.up.railway.app` targeting port 8080. Deploy and confirm `/units` returns 200 and the deploy logs show migrations applied.
- [ ] 5.3 Configure `expenses-frontend`: root `/FE`, config `/FE/railway.json`, `API_URL=https://expenses-api-production-4d79.up.railway.app`, `PREVIEW_ALLOWED_HOSTS=${{RAILWAY_PUBLIC_DOMAIN}},healthcheck.railway.app`. Deploy, open `https://expenses-frontend-production-2e52.up.railway.app`, and confirm the home screen loads with no CORS or 403 errors in the console and a receipt capture uploads.
